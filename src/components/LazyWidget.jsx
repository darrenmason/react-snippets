export default function LazyWidget() {
  return (
    <div className="stack">
      <strong>Lazy widget</strong>
      <p className="muted">This component loads only when needed.</p>
    </div>
  )
}

